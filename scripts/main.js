'use strict';

const greutate = prompt('Ce greutate ai (in kilograme)?');
const inaltime = prompt('Ce ianltime ai (in metri)?');
const rezultat = greutate / (inaltime ** 2);


function myFunction() {
    alert(rezultat.toFixed(2));

}

function myFunction2() {
    alert(rezultat.toFixed(2));
}